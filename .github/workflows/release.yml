name: Release

on:
  push:
    tags: ['v*']

permissions: {}

# Never cancel a running publish in favour of a newer tag
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      # npm trusted publishing needs npm 11.5.1 or later, which ships with Node 24
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Check tag, version and branch
        run: |
          if [[ ! "$GITHUB_REF_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Tag $GITHUB_REF_NAME is not a stable vMAJOR.MINOR.PATCH version"
            exit 1
          fi
          version="$(node -p "require('./package.json').version")"
          if [ "$GITHUB_REF_NAME" != "v$version" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match package.json version $version"
            exit 1
          fi
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::Tagged commit $GITHUB_SHA is not on main"
            exit 1
          fi
          npm_version="$(npm --version)"
          if [ "$(printf '%s\n' 11.5.1 "$npm_version" | sort -V | head -n1)" != 11.5.1 ]; then
            echo "::error::npm $npm_version is too old for trusted publishing, needs 11.5.1"
            exit 1
          fi
      - run: npm ci
      # prepublishOnly runs typecheck, tests and build. A version already on npm
      # from this exact commit means an earlier run published it and failed later,
      # so the rerun skips straight to the GitHub release.
      - name: Publish to npm
        run: |
          name="$(node -p "require('./package.json').name")"
          version="$(node -p "require('./package.json').version")"
          published_head="$(npm view "$name@$version" gitHead 2>/dev/null || true)"
          if [ -z "$published_head" ]; then
            npm publish --provenance
          elif [ "$published_head" = "$GITHUB_SHA" ]; then
            echo "$name@$version is already published from $GITHUB_SHA, skipping"
          else
            echo "::error::$name@$version is already published from $published_head, not from $GITHUB_SHA"
            exit 1
          fi
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            echo "Release $GITHUB_REF_NAME already exists"
          else
            gh release create "$GITHUB_REF_NAME" --verify-tag --generate-notes
          fi
